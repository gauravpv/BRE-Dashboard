# Optional: Microsoft-only login (hides email/password).
# SPRING_PROFILES_ACTIVE=prod,azure
# Entra credentials still come from AZURE_CLIENT_ID / AZURE_TENANT_ID / AZURE_CLIENT_SECRET.

opsconsole:
  auth:
    mode: azure
    seed-dev-users: false
